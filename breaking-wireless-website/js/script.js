// Custom JavaScript for Breaking Wireless

document.addEventListener('DOMContentLoaded', function() {
    console.log('Breaking Wireless site loaded.');

    // Future JavaScript enhancements can be added here.

    // Example: Initialize Bootstrap tooltips if used
    // var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
    // var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
    //   return new bootstrap.Tooltip(tooltipTriggerEl)
    // })

    // Reminder for Contact Form:
    // The contact form in contact.html requires a backend service or a third-party solution
    // like Formspree (https://formspree.io/) or Netlify Forms to actually send emails.
    // You will need to update the <form> tag's 'action' attribute with the endpoint provided by your chosen service.
    // Example using Formspree:
    // <form action="https://formspree.io/f/{your_form_id}" method="POST">
    // Replace {your_form_id} with your actual Formspree form ID.

});

